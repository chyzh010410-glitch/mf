server:
  port: 8082
spring:
  application:
    name: mf-product
  cloud:
    nacos:
      config:
        import-check:
          enabled: false
        password: nacos
        server-addr: localhost:8848
        username: nacos
      discovery:
        password: nacos
        server-addr: localhost:8848
        username: nacos
  config:
    import: optional:nacos:${spring.application.name}.yml
